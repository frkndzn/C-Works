#include <stdio.h>

int main(int argc, const char * argv[]) {
    int a;
    float p=3.14;
    float area;
    printf("enter radius");
    scanf("%d",&a);
    area=p*(a*a);
    printf("area : %f",area);
    
    
    return 0;
}
